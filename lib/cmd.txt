


      fluttergen -c pubspec.yaml;
      dart run build_runner build;dart pub global activate flutter_gen


   flutter pub get;   flutter pub run easy_localization:generate -S assets/translations -f keys -O lib/gen -o locale_keys.g.dart
      flutter pub cache clean
      fluttergen -c pubspec.yaml
      https://console.firebase.google.com/project/**YOUR_PROJECT**/settings/serviceaccounts/adminsdk
      ./gradlew signingReport
      git config --global user.email zzz@gmail.com ; git config --global user.name Khaled Elsherbiny
      git add .;
      git commit -m "
      - client issues file
      "
      ;git push

      flutter clean;flutter pub get;flutter build apk --release --no-tree-shake-icons

      git push origin stg:main

      git reset --hard commit_hash_you_want_to_return_to

      git checkout stg;git merge main;git push;flutter clean ;flutter pub get ;flutter build apk;git checkout main
      flutter pub upgrade --major-versions
      app links:
      flutter build apk --release --no-tree-shake-icons
      flutter build appbundle --no-tree-shake-icons
flutter build apk --release --no-tree-shake-icons --split-per-abi
git status;git add . ;git commit -m"orders";git push

flutter build apk --dart-define=APP_MODE=test